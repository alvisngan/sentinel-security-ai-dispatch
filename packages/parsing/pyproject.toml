[project]
name = "parsing"
version = "0.1.0"
description = "LLM-based shift-email parser"
requires-python = ">=3.12"
dependencies = [
    "openai>=2.29.0",
]

[build-system]
requires      = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/parsing"]
